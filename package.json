{
  "name": "colebemis.com",
  "version": "0.0.0",
  "author": "Cole Bemis <cole@colebemis.com>",
  "scripts": {
    "build": "rimraf build && mkdir build && node build.js",
    "format": "prettier --write '**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "cheerio": "^1.0.0-rc.2",
    "marked": "^0.5.1",
    "prettier": "^1.14.2",
    "rimraf": "^2.6.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/colebemis/colebemis.com"
  }
}
